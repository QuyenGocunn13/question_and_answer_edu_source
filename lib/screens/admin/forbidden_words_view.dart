import 'package:flutter/material.dart';

class ForbiddenWordsView extends StatelessWidget {
  const ForbiddenWordsView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(child: Text('Forbidden Words Management'));
  }
}
